[build]
  command = "npm run build:next"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Both Next.js and Vite are present; explicitly enable the Next.js runtime.
# It creates the server function and routes required by the storefront.
[[plugins]]
  package = "@netlify/plugin-nextjs"
