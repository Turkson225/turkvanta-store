'use client';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
export function FieldSelect({name,options,defaultValue,value,onValueChange,placeholder='Choose an option',required=false}:{name:string;options:(string|{value:string;label:string})[];defaultValue?:string;value?:string;onValueChange?:(v:string)=>void;placeholder?:string;required?:boolean}){return <Select name={name} defaultValue={defaultValue} value={value} onValueChange={onValueChange} required={required}><SelectTrigger className="w-full min-h-[45px] bg-background" aria-label={name.replaceAll('_',' ')}><SelectValue placeholder={placeholder}/></SelectTrigger><SelectContent>{options.map(o=>{const v=typeof o==='string'?o:o.value;return <SelectItem key={v} value={v}>{typeof o==='string'?o:o.label}</SelectItem>})}</SelectContent></Select>}
